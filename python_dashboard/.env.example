TELEGRAM_BOT_TOKEN=        # Telegram bot token
TELEGRAM_CHAT_ID=          # Telegram chat/group ID
SLACK_WEBHOOK_URL=         # Slack incoming webhook URL
VAPID_PRIVATE_KEY=         # VAPID private key for Web Push
VAPID_PUBLIC_KEY=          # VAPID public key (served to browsers)
VAPID_MAILTO=              # Contact email for VAPID (default: mailto:admin@example.com)

CARNAVAL_API_URL=http://192.168.40.101
FLASK_SECRET_KEY=change-me-to-a-random-string

WEBAUTHN_RP_ID=              # Relying Party ID, defaults to request hostname
WEBAUTHN_RP_NAME=            # Relying Party display name (default: Carnaval del Pais)
WEBAUTHN_ORIGIN=             # Expected origin (default: derived from request)
